{
  "name": "sanbuilder-gamekit",
  "version": "0.1.0",
  "private": false,
  "description": "H5 mobile game capability pack for Sanbuilder.",
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "validate": "node scripts/validate-gamekit.mjs",
    "test": "npm run validate && node --test tests/gamekit.test.mjs"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "yaml": "^2.9.0"
  }
}
